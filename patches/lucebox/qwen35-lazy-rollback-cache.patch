diff --git a/server/src/qwen35/qwen35_backend.cpp b/server/src/qwen35/qwen35_backend.cpp
index d37c1b7..1efc3de 100644
--- a/server/src/qwen35/qwen35_backend.cpp
+++ b/server/src/qwen35/qwen35_backend.cpp
@@ -1045,22 +1045,6 @@ int Qwen35Backend::do_prefill(const std::vector<int32_t> & tokens,
         std::fflush(stdout);
     }
 
-    // Skip KV-cache migration when resuming from a snapshot — the cache was
-    // already migrated when the snapshot was taken; re-running migrate would
-    // clobber the restored state.
-    if (kv_offset == 0) {
-        const int max_verify_tokens = cfg_.ddtree_mode
-            ? std::max<int>(dw_.block_size, cfg_.ddtree_budget + 1)
-            : dw_.block_size;
-        if (!migrate_prefill_cache(w_, cfg_.device.max_ctx,
-                                   max_verify_tokens,
-                                   target_backend_, cache_)) {
-            std::fprintf(stderr, "prefill: rollback cache migration failed: %s\n",
-                         dflash27b_last_error());
-            return -1;
-        }
-    }
-
     // Chunked prefill
     std::vector<float> embed_buf((size_t)hidden * prefill_ubatch);
     int committed = kv_offset;
@@ -1893,7 +1877,10 @@ bool Qwen35Backend::do_spec_decode(int committed, int n_gen,
     const int max_verify_tokens = cfg_.ddtree_mode
         ? std::max<int>(dw_.block_size, cfg_.ddtree_budget + 1)
         : dw_.block_size;
-    if ((cfg_.fast_rollback || cfg_.ddtree_mode) && !cache_.rollback_ctx) {
+    // Promote the prefill-only cache only after this request has qualified for
+    // speculative decoding. Plain chain verification snapshots recurrent
+    // state too, so every speculative mode needs rollback storage.
+    if (!cache_.rollback_ctx) {
         if (!migrate_prefill_cache(w_, cfg_.device.max_ctx,
                                    max_verify_tokens,
                                    target_backend_, cache_)) {
